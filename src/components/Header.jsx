import React from 'react';

const Titulo = (props) => {

    return (
        <div>
            <h1 className='p-3 mb-2 bg-secondary'>{props.titulo}</h1>
        </div>
    )


}

export default Titulo;
